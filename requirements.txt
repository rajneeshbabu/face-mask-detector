# ─────────────────────────────────────────────────────
# Face Mask Detector — Requirements
# Install with:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────

# Core ML
tensorflow>=2.15.0        # includes Keras 3 + TFLite converter
scikit-learn>=1.3.0

# Image processing
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Training utilities
matplotlib>=3.7.0         # training curves & charts

# Web app
gradio>=4.0.0

# Optional: lightweight TFLite inference (for production / deployment)
# tflite-runtime>=2.14.0  # uncomment if you don't want full tensorflow

# Optional: YOLOv8 comparison (requires PyTorch)
# ultralytics>=8.0.0      # pip install ultralytics
